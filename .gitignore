# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.suo
*.user

# Internal security notes — don't publish (it's a map of weaknesses)
SECURITY-REVIEW.md

# Claude Code — local, per-machine settings (paths, personal permissions)
.claude/settings.local.json
